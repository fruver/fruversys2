import React from 'react';
import Layout from '../components/Layout';

const Faq = () => {
  const title = 'Faq';

  return (
    <Layout title={title}>
      {title}
    </Layout>
  );
};

export default Faq;