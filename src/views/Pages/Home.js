import React from 'react';
import Layout from '../../components/Layout';

const Home = () => {
  const title = 'Panel';

  return (
    <Layout title={title}>
      {title}
    </Layout>
  );
};

export default Home;