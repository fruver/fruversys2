import * as React from 'react';
import Layout from '../../layouts/Layout';

const Summary = () => {
  const title = 'Panel';

  return (
    <Layout title={title}>
      Panel
    </Layout>
  );
};

export default Summary;