import * as React from 'react';
import Layout from '../../layouts/Dashboard';

const Dashboard = () => {
  const title = 'Panel';

  return (
    <Layout title={title}>
      Panel
    </Layout>
  );
};

export default Dashboard;